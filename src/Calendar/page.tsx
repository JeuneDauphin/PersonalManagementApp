// its a view file for the calendar page
// we will import the necessary components
// we will use a calendar component to display the calendar(Calendar.tsx)
// we will display a button to create a new event (Button.tsx)
// display the component search bar(Searchbar.tsx) to filter the events
// display the filter component(Filter.tsx) to filter the events by category
// display the date shortcuts component(DateShortcut.tsx) to quickly navigate to a specific date
// display the event list component(EventList.tsx) to see the list of events for the selected date
// display the EventCardPopup.tsx component to see the details of an event when clicked
// DateShortcut component shoud be located on the left of the calendar above the EventLists component
// EventList component should be located on the left of the calendar under the DateShortcut component


