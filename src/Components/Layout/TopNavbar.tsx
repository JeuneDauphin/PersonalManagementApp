// this component will be used as the top navigation bar
// it will contain the search bar component(Searchbar.tsx)
// it will contain the filter component(Filter.tsx)
// it will contain the button component(Button.tsx) to add new items
// it will be responsive and adapt to different screen sizes
// it will use Tailwind CSS for styling
// it will be used in main.tsx and wrap around the main content of the app