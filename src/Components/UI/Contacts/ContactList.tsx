//this component is the list of contacts, it will display a list of ContactCard components as a grid layout
// it will receive an array of contact objects as props and map through them to render each ContactCard component
// the component will also handle loading states and empty states
// Props:
// - contacts: array of contact objects to be displayed
// - isLoading: boolean to indicate if the data is still loading
// - onContactClick: function to handle click events on individual contact cards
// Example usage:
// <ContactList contacts={contacts} isLoading={isLoading} onContactClick={handleContactClick} />
// it is a controlled component that receives the list of contacts as props

