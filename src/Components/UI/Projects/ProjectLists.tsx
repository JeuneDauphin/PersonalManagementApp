//The ProjectList component will display a list of ProjectCard components in a grid layout.
  //It will receive an array of project objects as props and map through them to render each ProjectCard.
  //The component will also handle loading states and empty states.
  //Props:
  //projects: Array of project objects to be displayed.
  //isLoading: Boolean to indicate if the data is still loading.
  //onProjectClick: Function to handle click events on individual project cards.
  //Example usage:
  // <ProjectList projects={projects} isLoading={isLoading} onProjectClick={handleProjectClick} />
