// this is the generic searchbar component used in multiple places in the app (contacts, tasks, schools, calendar, projects)
 // interface:
 // - placeholder: string to display as placeholder text in the search input
 // - value: string to display as the current search query
 // - onChange: function to handle search query change
 // Example usage:
 // <Searchbar
 //   placeholder="Search contacts..."
 //   value={searchQuery}
 //   onChange={handleSearchChange}
 // />
 // it is a controlled component that receives the search query and onChange function as props  